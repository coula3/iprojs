<% if !logged_in? %>
    <a href="/">Back</a>
<% end %><br><br>

<% if logged_in? %>
    <%if rand(4) != 0%>
        <h2>Hey <%= current_user.first_name.capitalize %>!</h2>
    <% else %>
        <h2>Hey Buddy!</h2>
    <% end %>
<% end %>

<% if !logged_in? %>
    <h2>About</h2>
<% end %>

<div>
    <p><span style="color: red">iProjs</span> is a platform that provides software developers a useful tool to enrich their development experience. This ease-of-use medium has functionalities to hold key data on project portfolio that typically requires frequent access.</p>

    <p>The average times to create or update a project record is estimated at less than 2 minutes.</p> 

    <p>That makes <span style="color: red">iProjs</span> an ideal buddy of software developers in the cloud!</p>

    <p>We trust that your interaction with the product will be consistently valuable.</p>

    <% if logged_in? && current_user.projects.empty?%>
        <p>Let's get started with a <a href="/projects/new">new project</a>!!</p>
    <% end %>
</div>